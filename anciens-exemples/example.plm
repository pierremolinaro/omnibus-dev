enum Toto @size8 {
  case a
  case b
  case c
}

proc truc !out: Toto azert ?!inout: Toto par {
  var Toto azerty = .a
  Toto xyz = .a
  azert = .b
  par = .c
}

proc machin {
  Toto par1
  Toto par2 = .c
  truc (?out:par1 !?inout:par2)
  Bool bb ; bb = par1 < par2
  Bool cc = Bool.false & .true
  if par1 > par2 then
    Bool xyz = .false
  elsif par1 < par2 then
    Bool xyz_elseif = .false
  else
    Bool xyz_else = .false
  end
}
