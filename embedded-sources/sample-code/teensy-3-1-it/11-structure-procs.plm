target "teensy-3-1-it"

//------------------------------------------------*

struct $MyStruct {
  var x $int32 = 9
  var y $int32 = 9

  proc nop `user () {
  }
//  proc getX `user (!outX $int32) {
//    outX = x 
//  }
}

//------------------------------------------------*

proc setup `user () {
}

//------------------------------------------------*

proc loop `user () {
  var s = $MyStruct ()
  s.nop ()
//  var x $int32 ; s.getX (?x)
}

//------------------------------------------------*
