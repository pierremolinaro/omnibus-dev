target "teensy-3-1-it"

//------------------------------------------------*

proc setup `user () {
}

//------------------------------------------------*

proc loop `user () {
  waitMS (!250)
  ledOn (!LED_L1) // Allumer la led
  waitMS (!250)
  ledOff (!LED_L1)  // Éteindre la led
}

//------------------------------------------------*
