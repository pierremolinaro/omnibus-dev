target "teensy-3-1-it"

//------------------------------------------------*

enum $ledState {
  case on
  case off
}

//------------------------------------------------*

proc setup `user () {
}

//------------------------------------------------*

var led = $ledState.on {
  @rw proc loop
}

//------------------------------------------------*

proc loop `user () {
  waitMS (!250)
  if led == .off then
    ledOn (!LED_L1) // Allumer la led
    led = .on
  else
    ledOff (!LED_L1)  // Éteindre la led
    led = .off
  end
}

//------------------------------------------------*
