target "teensy-3-1-it"

//------------------------------------------------*

task T priority 12 stackSize 512 {
  var gCompteur $uint32 = 0
  proc setup {
  }
  proc loop {
    waitUntilMS (!deadline:gCompteur)
    ledOn (!LED_L1) // Allumer la led
    gCompteur +%= 500
    waitUntilMS (!deadline:gCompteur)
    ledOff (!LED_L1) // Allumer la led
    gCompteur +%= 500
    goto (!line:0 !column:0)
    printUnsigned (!millis ())
  }
}


//------------------------------------------------*
