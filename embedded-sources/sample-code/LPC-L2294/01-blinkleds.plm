target "LPC-L2294"

//------------------------------------------------*

task T1 priority 1 stackSize 512 {
  var compteur $uint32 = 0

  func loop () {
    //waitUntilMS (!deadline:self.compteur)
//    IO1CLR  = 1 << 23 // Led On
    // ledOn (!LED_L0)
    self.compteur +%= 500
    // waitUntilMS (!deadline:self.compteur)
    // ledOff (!LED_L0)
//    IO1SET  = 1 << 23 // Led Off
    self.compteur +%= 500
  }
}

//------------------------------------------------*
